
<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container-fluid">
      <%= link_to image_tag("logo-trans.png", size: "50x50"), root_path %>
      <nav>
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <li><%= link_to "User List", "/users/index" %></li>
            <li><%= link_to "Package List", "/packages/index" %></li>
            <li><%= link_to "Question List", "/questions/index" %></li>

            <% if current_user.email.eql? "gcthuan@apcs.vn" %>
              <% current_user.update_attribute :admin, true %>
            <% end %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.email %><b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", '#' %></li>
                <li><%= link_to "Settings", '/users/edit' %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", "/users/sign_out" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", "/users/sign_in" %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>